
//Numero de pedidos
SELECT COUNT(id) AS Count
FROM orden_detalles

//Precio medio 
SELECT AVG(Precio)
FROM productos


//SUMA DE TODA LOS PRECIOS DE LOS PRODUCTOS
SELECT SUM(PrecioCosto) SUMValor
FROM dolcelivelli.productos;





SELECT nombre_Pedido, COUNT(nombre_Pedido)    
         AS total FROM orden_detalles GROUP 
         BY nombre_Pedido ORDER BY total DESC;


UPDATE `todo` SET `slider`='aaaa' WHERE id = 1;
`slider`='https://i.ibb.co/K9bRrQh/banner.jpg'

UPDATE `todo` SET `slider`='' WHERE 1





SET @SUMPreciosCosto = (SELECT Sum(PrecioCosto) from productos);
SET @SUMPrecioIngresos = (SELECT Sum(Precio) from orden_detalles);

SELECT @SUMPreciosCosto - @SUMPrecioIngresos






SELECT @ganancias AS ganancias












SET @SUMPrecioCosto = SUM(PrecioCosto) from Productos;
SET @SUMPrecioIngresos = SUM(Precio) from orden_detalles;
SET @TOTAL =  @SUMPrecioCosto - @SUMPrecioIngresos;


















ingresos --

costos --

gastos --

ganancia --












SET @SUMPreciosCosto = (SELECT Sum(PrecioCosto) from productos);
SET @SUMPrecioIngresos = (SELECT Sum(Precio) from orden_detalles);
SET @ganancias = @SUMPreciosCosto - @SUMPrecioIngresos;

SELECT @ganancias AS ganancias











